#pragma once
#include <glm/fwd.hpp>
namespace eldr::em {
template <typename Value_, std::size_t Size_> struct Matrix;
template <typename Value_, std::size_t Size_> struct Vector;
template <typename Value_, std::size_t Size_> struct Normal;
template <typename Value_, std::size_t Size_> struct Point;
template <typename Point> struct Transform;
} // namespace eldr::em
